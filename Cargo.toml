[package]
name = "arbitus"
version = "1.0.0"
edition = "2024"
description = "Security proxy for MCP (Model Context Protocol) — auth, rate limiting, payload filtering, and audit logging between AI agents and MCP servers"
homepage = "https://arbitus-gateway.xyz"
documentation = "https://arbitus-gateway.xyz/docs"
repository = "https://github.com/arbitusgateway/arbitus"
license = "AGPL-3.0-or-later"
keywords = ["mcp", "proxy", "security", "ai", "gateway"]
categories = ["network-programming", "authentication"]

[lib]
name = "arbitus"
path = "src/lib.rs"

[[bin]]
name = "dummy-server"
path = "src/bin/dummy_server.rs"

[[bin]]
name = "arbitus"
path = "src/bin/arbitus.rs"

[features]
default = ["kubernetes"]
kubernetes = ["dep:kube", "dep:k8s-openapi"]

[dependencies]
clap = { version = "4", features = ["derive"] }
axum = "0.8"
axum-server = { version = "0.7", features = ["tls-rustls"] }
tokio = { version = "1", features = ["full"] }
tokio-stream = "0.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"
regex = "1"
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls", "stream"] }
async-trait = "0.1"
anyhow = "1"
rusqlite = { version = "0.31", features = ["bundled"] }
uuid = { version = "1", features = ["v4"] }
prometheus = "0.14"
futures-util = "0.3"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }
tracing-opentelemetry = "0.27"
opentelemetry = { version = "0.26", features = ["trace", "metrics", "logs"] }
opentelemetry_sdk = { version = "0.26", features = ["rt-tokio", "metrics", "logs"] }
opentelemetry-otlp = { version = "0.26", features = ["trace", "metrics", "logs", "grpc-tonic"] }
opentelemetry-appender-tracing = "0.26"
jsonwebtoken = { version = "10", features = ["rust_crypto"] }
subtle = "2"
ra2a = { version = "0.9.3", features = ["server"] }
chrono = { version = "0.4", default-features = false, features = ["clock"] }
base64 = "0.22"
percent-encoding = "2"
unicode-normalization = "0.1"
jsonschema = { version = "0.18", default-features = false }
sha2 = "0.10"
hex = "0.4"
which = "7"
lru = "0.16.3"
governor = "0.6"
kube = { version = "0.98", features = ["client", "runtime"], optional = true }
k8s-openapi = { version = "0.24", features = ["v1_31"], optional = true }
regorus = "0.2"
tower = "0.5"
tokio-rustls = "0.26"
rustls = { version = "0.23", features = ["ring"] }
rustls-pemfile = "2"
x509-parser = "0.16"

[dev-dependencies]
libc = "0.2"
tokio = { version = "1", features = ["full", "test-util"] }
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
axum = "0.8"
serde_json = "1"
futures-util = "0.3"
rusqlite = { version = "0.31", features = ["bundled"] }
tempfile = "3"
